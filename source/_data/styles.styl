// 1. 强制圆角：针对 Gemini 高版本对直角的顽固设置进行全面“打磨”
.post-block,
.main-inner,
.header,
.header-inner,
.sidebar-inner,
.sidebar,
.comments,
.pagination,
.footer-inner,
.widget-wrap,
.site-nav,
.column,
.search-popup {
  border-radius: 15px !important;
  overflow: hidden !important; 
  box-shadow: 0 4px 15px rgba(0,0,0,0.05) !important;
}

// 特别锁定页眉与导航菜单（Gemini 方案中 site-nav 及其内部 menu 都是独立层）
.site-nav,
.site-nav .menu {
  border-radius: 0 0 15px 15px !important;
}
.site-brand-container {
  border-radius: 15px 15px 0 0 !important;
}

// 消除容器直角，但保留组件内部配色
.header,
.column {
  background: transparent !important;
  box-shadow: none !important;
}
.site-brand-container,
.site-nav {
  // 亮色下默认为白
  background: var(--content-bg-color) !important;
}

// 2. 彻底禁用头像旋转
.site-author-image {
  animation: none !important;
  transition: transform 0.3s ease-out !important;
  &:hover {
    transform: scale(1.1) !important;
  }
}

// 3. 【统一底部配色】消除区域偏差
.footer {
  background: transparent !important;
  color: var(--text-color) !important;
}
.footer-inner {
  background: transparent !important; 
  box-shadow: none !important;
}

// 4. 【高性能暗黑模式】变量集
:root[data-theme="dark"] {
  --body-bg-color: #1a1a1a;
  --content-bg-color: #242424;
  --card-bg-color: #242424;
  --text-color: #ddd;
  --link-color: #88d;
  --link-hover-color: #aaf;
  --brand-color: #ddd;
  --blockquote-color: #999;
  --btn-default-bg: #333;
  --btn-default-color: #eee;
  --btn-default-border-color: #444;
  --menu-item-bg-color: #333;
  
  body {
    background: #1a1a1a !important;
    color: #ddd !important;
  }
  
  .main-inner, .sidebar-inner, .post-block, .site-brand-container, .site-nav, .search-popup {
    background: #242424 !important;
    box-shadow: 0 4px 20px rgba(0,0,0,0.4) !important;
  }
  
  .site-title, .site-author-name, .menu-item a, .site-subtitle {
    color: #eee !important;
  }

  // 强化激活/暗黑菜单对比度
  .menu-item .menu-item-active a, .menu-item a:hover {
    background: #333 !important;
    color: #fff !important;
  }
  
  canvas {
    opacity: 0.8;
  }
}
